>hdp.sh 启动停止hadoop集群
>zk.sh 启动停止zookeeper集群
>kf.sh 启动停止kafka集群
>f1.sh f2.sh 启动两个不同的flume程序
>cluster.sh 启动hadoop、zookeeper、kafka、flume程序
>superset.sh 启动superset，gunicorn位于web服务器和客户端之间。
>xsync 分发文件到集群机器
>xcall.sh 在所有机器上执行同一个命令，如"xcall.sh jps"。